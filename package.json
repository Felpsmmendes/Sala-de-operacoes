{
  "name": "sala-de-operacoes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:eventos": "npm run dev -w @sala/eventos",
    "dev:painel": "npm run dev -w @sala/painel",
    "lint": "oxlint apps",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
